<!-- 改编自：园艺主题项目提示词/gb-61/植物养护百科平台.md；后端由 Spring Boot 3 + Java 17 + MyBatis-Plus 改造为 Go 1.22 + Gin + GORM -->
# gb-61 植物养护知识百科平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**植物养护知识百科平台（gbplantwiki）** 为园艺爱好者提供全面的植物养护指南，涵盖常见花卉、绿植、多肉等品种的养护方法、病虫害防治、季节管理与问答互动，支持图文并茂的知识展示、个人花园与养护提醒。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, username, email, password_hash, nickname, avatar, bio, role（user/admin）, created_at
  - 关键关系：拥有多个 Favorite、UserGarden、CareReminder、Question 和 Answer
  - 业务动作：注册/登录、修改资料、上传头像
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.vue**

- **PlantSpecies（植物品种）**
  - 字段：id, family, genus, name, alias, type（观花/观叶/多肉/水生）, origin, temp_min, temp_max, light_requirement, water_frequency, description, image_urls（JSON）, created_at
  - 关键关系：可被多个 Favorite 收藏、被多个 UserGarden 拥有、关联 DiseasePest
  - 业务动作：品种库浏览/筛选、品种详情展示、收藏、加入我的花园
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **CareArticle（养护知识文章）**
  - 字段：id, user_id, title, content, cover, topic_tag（施肥/修剪/换盆/病虫害/繁殖）, status, view_count, created_at, updated_at
  - 关键关系：隶属于 User，可被多个 Favorite 收藏
  - 业务动作：富文本发布/编辑、按主题标签筛选、收藏与阅读
  - **此实体必须贯穿全栈**

- **DiseasePest（病虫害诊断）**
  - 字段：id, plant_species_id, name, symptoms, cause, treatment, recommended_medicine, images（JSON）, keywords, created_at
  - 关键关系：可关联 PlantSpecies
  - 业务动作：病虫害手册浏览、按品种/症状关键词搜索
  - **此实体必须贯穿全栈**

- **CareReminder（养护提醒计划）**
  - 字段：id, user_id, plant_species_id, task_title, remind_date, frequency, status（pending/done/overdue）, created_at
  - 关键关系：隶属于 User，可关联 PlantSpecies
  - 业务动作：创建养护计划、到期站内消息提醒、标记完成
  - **此实体必须贯穿全栈**

- **Favorite（收藏）**
  - 字段：id, user_id, target_type（plant/article）, target_id, created_at
  - 关键关系：连接 User 与 PlantSpecies/CareArticle
  - 业务动作：收藏/取消收藏、个人收藏夹展示
  - **此实体必须贯穿全栈**

- **UserGarden（我的花园）**
  - 字段：id, user_id, plant_species_id, nickname, owned_since, location, care_reminder_id, created_at
  - 关键关系：隶属于 User，关联 PlantSpecies 与 CareReminder
  - 业务动作：建立我的花园清单、绑定养护计划
  - **此实体必须贯穿全栈**

- **Question（问答问题）**
  - 字段：id, user_id, title, content, images（JSON）, status, created_at
  - 关键关系：隶属于 User，拥有多个 Answer
  - 业务动作：发布养护问题（附图片）、采纳最佳回答
  - **此实体必须贯穿全栈**

- **Answer（回答）**
  - 字段：id, question_id, user_id, content, is_best, like_count, created_at
  - 关键关系：隶属于 Question 与 User
  - 业务动作：回答问题、点赞、被采纳为最佳回答
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/（首页）**
  - 消费实体：PlantSpecies、CareArticle、DiseasePest、CareReminder
  - 关键交互：热门品种、最新养护文章、当季养护重点任务、搜索入口
  - 共享组件：`<PlantCard>`、`<CareArticleCard>`

- **/plants（品种库）**
  - 消费实体：PlantSpecies
  - 关键交互：按科属/类型筛选、关键词搜索、品种卡片浏览
  - 共享组件：`<PlantCard>`（与 / 共用）、`<SearchFilter>`

- **/plants/:id（品种详情）**
  - 消费实体：PlantSpecies、Favorite、UserGarden、DiseasePest
  - 关键交互：详细信息卡片、收藏、加入我的花园、关联病虫害
  - 共享组件：`<FavoriteButton>`、`<ImageCarousel>`

- **/articles（养护知识文章）**
  - 消费实体：CareArticle
  - 关键交互：按主题标签筛选、文章列表分页
  - 共享组件：`<CareArticleCard>`（与 / 共用）、`<SearchFilter>`

- **/articles/:id（文章详情）**
  - 消费实体：CareArticle、Favorite
  - 关键交互：富文本渲染、收藏、返回列表
  - 共享组件：`<FavoriteButton>`（与 /plants/:id 共用）

- **/pests（病虫害诊断手册）**
  - 消费实体：DiseasePest
  - 关键交互：图文对照表、按品种/症状关键词搜索
  - 共享组件：`<SearchFilter>`、`<DiseaseCard>`

- **/calendar（季节养护日历）**
  - 消费实体：CareReminder、PlantSpecies
  - 关键交互：按当前月份展示当季任务、创建养护提醒
  - 共享组件：`<ReminderList>`

- **/garden（我的花园）**
  - 消费实体：UserGarden、CareReminder、Favorite
  - 关键交互：花园清单、绑定养护计划、收藏夹管理
  - 共享组件：`<ReminderList>`（与 /calendar 共用）、`<PlantCard>`

- **/questions（问答互动社区）**
  - 消费实体：Question、Answer、User
  - 关键交互：发布问题、回答问题、点赞、采纳最佳回答
  - 共享组件：`<QuestionItem>`

- **/quiz（养护知识小测验）**
  - 消费实体：User、Question（测验题）
  - 关键交互：答题、交卷、展示成绩与错题解析
  - 共享组件：`<QuizCard>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中用户私有接口需 `AuthRequired()`，管理员维护品种/文章需 `RequireRole(admin)`
    - 前端：`router/guards.ts`、`components/common/RoleGuard.vue`、`stores/authStore.ts`

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器

- **接口限流**
  - 触达文件层：
    - 后端：`internal/middleware/rate_limiter.go`
    - 路由：登录、发布问题、上传图片等接口启用限流
    - 配置：`internal/config/config.go`

- **文件上传（本地静态资源）**
  - 触达文件层：
    - 后端：`internal/handler/upload_handler.go`、`internal/service/upload_service.go`、`internal/util/file.go`
    - 路由：图片上传接口
    - 前端：`components/common/ImageUploader.vue`、`utils/request.ts`
    - Nginx：`frontend/nginx.conf` 静态资源代理

### 5. 共享枚举/常量（≥ 2 组）

- **PlantType（植物类型）**
  - 值：FLOWER = 'flower'（观花）, FOLIAGE = 'foliage'（观叶）, SUCCULENT = 'succulent'（多肉）, AQUATIC = 'aquatic'（水生）
  - 前端定义位置：`frontend/src/constants/plant.ts`
  - 后端定义位置：`backend/internal/constants/plant.go`
  - 出现位置：PlantSpecies 模型、PlantService、PlantCard、品种库筛选器、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **CareTopicTag（养护主题标签）**
  - 值：FERTILIZING = 'fertilizing'（施肥）, PRUNING = 'pruning'（修剪）, REPOTTING = 'repotting'（换盆）, PEST_CONTROL = 'pest_control'（病虫害）, PROPAGATION = 'propagation'（繁殖）
  - 前端定义位置：`frontend/src/constants/article.ts`
  - 后端定义位置：`backend/internal/constants/article.go`
  - 出现位置：CareArticle 模型、ArticleService、CareArticleCard、文章筛选器、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **FavoriteTargetType（收藏目标类型）**
  - 值：PLANT = 'plant', ARTICLE = 'article'
  - 前端定义位置：`frontend/src/constants/favorite.ts`
  - 后端定义位置：`backend/internal/constants/favorite.go`
  - 出现位置：Favorite 模型、FavoriteService、FavoriteButton、收藏夹列表、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<PlantCard>`：被 /、/plants、/garden 共用
- `<CareArticleCard>`：被 / 和 /articles 共用
- `<FavoriteButton>`：被 /plants/:id 和 /articles/:id 共用
- `<SearchFilter>`：被 /plants、/articles、/pests 共用
- `<ReminderList>`：被 /calendar 和 /garden 共用
- `<ImageCarousel>`：被 /plants/:id 使用
- `<DiseaseCard>`：被 /pests 使用
- `<QuestionItem>`：被 /questions 使用
- `<QuizCard>`：被 /quiz 使用
- `<ImageUploader>`：被文章发布与问题发布使用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：登录态与角色判断
- `useFavorite()`：收藏/取消收藏状态管理
- `useReminderStats()`：养护提醒统计
- `useQuiz()`：测验作答与成绩计算
- `utils/dateFormat.ts`：日期时间格式化
- `utils/season.ts`：根据月份返回当季养护任务

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `rate_limiter.go`：接口限流
- `error_handler.go`：统一错误响应格式
- `logger.go`：请求日志记录
- `cors.go`：跨域配置

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`gbplantwiki`
- **前端端口**：8102
- **后端端口**：3102
- **前端框架**：Vue 3 + TypeScript
- **UI 库**：Element Plus
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, plant.ts, article.ts, pest.ts, reminder.ts, favorite.ts, garden.ts, question.ts
├── stores/           # authStore.ts, userStore.ts, plantStore.ts, articleStore.ts, reminderStore.ts
├── types/            # 共享类型定义
├── components/common/# PlantCard, CareArticleCard, FavoriteButton, SearchFilter, ReminderList, ImageCarousel, DiseaseCard, QuestionItem, QuizCard, ImageUploader
├── hooks/            # useAuth.ts, useFavorite.ts, useReminderStats.ts, useQuiz.ts
├── pages/            # Home, PlantLibrary, PlantDetail, ArticleList, ArticleDetail, PestManual, SeasonCalendar, Garden, QuestionCommunity, Quiz, Profile, Login
├── router/           # index.ts + guards.ts
├── utils/            # dateFormat.ts, season.ts, request.ts
└── constants/        # plant.ts, article.ts, favorite.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、限流、端口、上传目录等）
    ├── model/                   # 按实体分文件：user.go, plant_species.go, care_article.go, disease_pest.go, care_reminder.go, favorite.go, user_garden.go, question.go, answer.go
    ├── repository/              # 按实体分文件：user_repository.go, plant_species_repository.go, care_article_repository.go, disease_pest_repository.go, care_reminder_repository.go, favorite_repository.go, user_garden_repository.go, question_repository.go, answer_repository.go
    ├── service/                 # 按实体分文件：user_service.go, plant_species_service.go, care_article_service.go, disease_pest_service.go, care_reminder_service.go, favorite_service.go, user_garden_service.go, question_service.go, answer_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, plant_species_handler.go, care_article_handler.go, disease_pest_handler.go, care_reminder_handler.go, favorite_handler.go, user_garden_handler.go, question_handler.go, answer_handler.go, upload_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, plants.go, articles.go, pests.go, reminders.go, favorites.go, gardens.go, questions.go
    ├── middleware/              # auth.go, rbac.go, rate_limiter.go, error_handler.go, logger.go, cors.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # plant.go, article.go, favorite.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, file.go, season.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `PLANT_FAVORITE_SUCCESS`、`REMINDER_CREATE_FAILED`
   - 任何业务字段变更（如 PlantSpecies 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"PlantSpecies[id=123] favorite failed: target type not match"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、温度格式化、状态文本、类型文本、养护主题文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **养护提醒状态机必须跨多处定义**
   - CareReminder 状态的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - PlantType、CareTopicTag 和 FavoriteTargetType 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 PlantSpecies 新增一个 'air_purifying' 净化空气标签"，必须触达：GORM AutoMigrate、PlantSpecies 模型、前后端 constants、PlantService、PlantCard、SearchFilter、formatters、日志模板、错误码、首页推荐逻辑、筛选器等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbplantwiki`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbplantwiki`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-gbplantwiki}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `8102:80`，后端 `${BACKEND_PORT:-3102}:8080`，数据库 `${DB_PORT:-3502}:3306`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbplantwiki`
- 数据库连接相关：`DB_NAME=gbplantwiki_db`、`DB_USER=gbplantwiki_user`、`DB_PASSWORD=gbplantwiki_pwd`、`DB_ROOT_PASSWORD=gbplantwiki_root`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=8102`、`BACKEND_PORT=3102`、`DB_PORT=3502`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 PlantType、CareTopicTag 和 FavoriteTargetType 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:8102` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<PlantCard>` 或 `<FavoriteButton>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求

# BUG_REPRO: 上传报成功但没图、偶发无响应、残留文件

## Bug 是什么
- handler 把上传失败的错误吞成 200 成功（url 为空），页面显示成功但没图；
- 失败路径不清理残留文件，磁盘堆积；
- panic 恢复只记日志不返回响应，客户端悬挂。

## 如何触发
1. 启动服务并登录；
2. 上传不支持的文件类型（如 .exe）或空文件；
3. 触发 handler panic。

## 真实错误信息
- 失败上传返回 200，响应体 `{"code":0,"message":"ok","data":{"url":""}}`；
- 上传目录残留未清理的小文件；panic 后无任何响应。

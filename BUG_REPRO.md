# BUG_REPRO: 被跳过的养护提醒被逾期扫描翻回

## Bug 是什么
- 用户把提醒标记为 `skipped`（跳过）后，逾期扫描把 skipped 一并翻成 `overdue`；
- 活跃列表未排除 skipped，跳过的提醒仍显示；已完成提醒还能被跳过（非法迁移）。

## 如何触发
1. 启动服务并登录；
2. 对提醒执行状态更新为 skipped；
3. 再次拉取 `GET /api/v1/reminders` 或触发 `MarkOverdue`。

## 真实错误信息
- 跳过后的提醒再次出现在列表中且状态为 overdue；
- 对已完成提醒调用跳过接口未被拒绝（返回 200）。

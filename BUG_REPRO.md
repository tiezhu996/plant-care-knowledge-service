# BUG_REPRO: 我的花园列表条目串数据

## Bug 是什么
- 花园列表在计算“绑了提醒”子集时用 `FilterInPlace` 原地压缩共享底层数组；
- 返回的原始切片被污染，删除一个绑定提醒的条目后其他条目也跟着消失/重复。

## 如何触发
1. 启动服务并登录；
2. 花园里添加多条记录（部分绑定提醒）；
3. 调用 `GET /api/v1/gardens` 观察返回列表。

## 真实错误信息
- 列表返回条目少于实际数量或出现重复条目；刷新后恢复。

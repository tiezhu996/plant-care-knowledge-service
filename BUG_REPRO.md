# BUG_REPRO: 收藏列表请求取消不生效且互相污染

## Bug 是什么
- 取消/超时的收藏列表请求仍会继续查库，页面一直转圈；
- 仓储把首个请求的 ctx 存进结构体复用，后续请求复用旧 ctx 导致互相污染。

## 如何触发
1. 启动服务并登录；
2. 发起 `GET /api/v1/favorites` 后立即取消请求（或带短超时 ctx）。

## 真实错误信息
- 取消的请求返回 200 并正常查询（应中止）；
- 第二个健康请求因复用了首个被取消的 ctx 报 `context canceled`。
